{
  "name": "sandhya",
  "private": true,
  "version": "0.1.0",
  "description": "A local dual-tradition daily reader and journal — Stoic philosophy paired with Bhagavad Gita / Srimad Devi Bhagavatam teachings.",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run build && npm start --prefix server",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
